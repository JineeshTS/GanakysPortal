class: Notification
table: notifications
fields:
  userId: int, relation(parent=users)
  title: String
  message: String
  type: String
  isRead: bool, default=false
  data: String?
  createdAt: DateTime
indexes:
  notif_user_idx:
    fields: userId
  notif_read_idx:
    fields: userId, isRead
